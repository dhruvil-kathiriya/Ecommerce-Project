<%- include("./Admin_Header") %>

    <section class="content-main">
        <div class="content-header">
            <div>
                <h2 class="content-title card-title">Admins </h2>
                <p>Add, edit or delete a Admins</p>
            </div>
            <div>
                <form>
                    <div class="d-flex align-items-center">
                        <input type="text" placeholder="Search Admins" class="form-control bg-white" name="search"
                            value="<%= searchValue %>">
                        <button type="submit" class="btn btn-md m-2 rounded font-sm hover-up"
                            value="search">Search</button>
                    </div>
                </form>
            </div>
        </div>
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-12">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th class="text-center align-middle">
                                            <div class="form-check">
                                                <input class="form-check-input" type="checkbox" value="" />
                                            </div>
                                        </th>
                                        <th class="text-center align-middle">Image</th>
                                        <th class="text-center align-middle">Name</th>
                                        <th class="text-center align-middle">Email</th>
                                        <th class="text-center align-middle">Gender</th>
                                        <th class="text-center align-middle">Hobby</th>
                                        <th class="text-center align-middle">City</th>
                                        <th class="text-center align-middle">Description</th>
                                        <th class="text-center align-middle">Status</th>
                                        <th class="text-center align-middle">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if(adminData !="" ){ %>
                                        <% for(var ad of adminData ){ %>
                                            <tr class="">
                                                <td class="text-center align-middle">
                                                    <div class="form-check">
                                                        <input class="form-check-input" type="checkbox" value="" />
                                                    </div>
                                                </td>
                                                <td class="text-center align-middle">
                                                    <img src="<%= ad.admin_image %>" alt="<%= ad.admin_image %>"
                                                        class="rounded-circle" height="50" width="50">
                                                </td>
                                                <td class="text-center align-middle">
                                                    <%= ad.name %>
                                                </td>
                                                <td class="text-center align-middle"><strong>
                                                        <%= ad.email %>
                                                    </strong></td>
                                                <td class="text-center align-middle">
                                                    <%= ad.gender %>
                                                </td>
                                                <td class="text-center align-middle">
                                                    <%= ad.hobby %>
                                                </td>
                                                <td class="text-center align-middle">
                                                    <%= ad.city %>
                                                </td>
                                                <td class="text-center align-middle">
                                                    <%= ad.description %>
                                                </td>
                                                <td class="text-center align-middle">
                                                    <%if(ad.isActive){%>
                                                        <a href="/admin/isActive/<%=ad.id%>"
                                                            class="badge rounded-pill alert-success">Active</a>
                                                        <% }else{ %>
                                                            <a href="/admin/deActive/<%=ad.id%>"
                                                                class="badge rounded-pill alert-danger">Inactive</a>
                                                            <% }%>
                                                </td>
                                                <td class="text-center align-middle">
                                                    <div class="dropdown">
                                                        <a class="dropdown-item text-secondary border rounded"
                                                            href="/admin/updateAdmin/<%=ad.id%>"><i
                                                                class="material-icons md-edit"></i>Update</a>
                                                    </div>
                                                    <div class="dropdown">
                                                        <a class="dropdown-item text-danger border rounded"
                                                            href="/admin/deleteAdmin/<%=ad.id%>"><i
                                                                class="material-icons md-delete_forever"></i>Delete</a>
                                                    </div>
                                                </td>
                                            </tr>
                                            <% }%>
                                                <% }else{ %>
                                                    <td class="text-center align-middle" colspan="7">
                                                        <p class="text-center text-danger">No Record Found</p>
                                                    </td>
                                                    <% } %>
                                </tbody>
                            </table>

                            <% if(currentPage> 0){ %>
                                <a href="?pages=<%= currentPage - 1 %>&search=<%= searchValue %>"
                                    class="btn btn-primary" style="padding: 10px 16px;">
                                    Previous</a>
                                <% } %>
                                    <% if(currentPage> 0){ %>
                                        <a href="?pages=<%= currentPage - 1 %>&search=<%= searchValue %>"
                                            class="btn border" style="padding: 10px 16px;">
                                            <%= currentPage %>
                                        </a>
                                        <% } %>
                                            <a href="?pages=<%= currentPage %>&search=<%= searchValue %>"
                                                class="btn btn-primary" style="padding: 10px 16px;">
                                                <%= currentPage + 1 %>
                                            </a>
                                            <% if(totalDocument> currentPage + 1){ %>
                                                <a href="?pages=<%= currentPage + 1 %>&search=<%= searchValue %>"
                                                    class="btn border" style="padding: 10px 16px;">
                                                    <%= currentPage + 2 %>
                                                </a>
                                                <% } %>
                                                    <% if(totalDocument> currentPage + 1){ %>
                                                        <a href="?pages=<%= currentPage + 1 %>&search=<%= searchValue %>"
                                                            class="btn btn-primary" style="padding: 10px 16px;">Next</a>
                                                        <% } %>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <%- include("./Admin_Footer") %>